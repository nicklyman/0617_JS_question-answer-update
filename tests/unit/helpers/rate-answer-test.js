import { rateAnswer } from 'questions-answers/helpers/rate-answer';
import { module, test } from 'qunit';

module('Unit | Helper | rate answer');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = rateAnswer([42]);
  assert.ok(result);
});
