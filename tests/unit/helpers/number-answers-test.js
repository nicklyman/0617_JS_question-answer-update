import { numberAnswers } from 'questions-answers/helpers/number-answers';
import { module, test } from 'qunit';

module('Unit | Helper | number answers');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = numberAnswers([42]);
  assert.ok(result);
});
