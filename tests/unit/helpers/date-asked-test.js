import { dateAsked } from 'questions-answers/helpers/date-asked';
import { module, test } from 'qunit';

module('Unit | Helper | date asked');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateAsked([42]);
  assert.ok(result);
});
